/*
import { playwrightRollup, expectSuccess } from '@~local/playwright-tooling';
import { test } from '@playwright/test';

playwrightRollup();

test.describe('scrollbarsSetup.scrollTimeline', () => {
  test('scrollTimeline', async ({ page }) => {
    await expectSuccess(page);
  });
});
*/
